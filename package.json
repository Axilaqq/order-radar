{
  "name": "order-radar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Сборщик фриланс-заказов с площадок в Telegram",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test \"test/*.test.js\"",
    "check-sources": "node scripts/check-sources.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
